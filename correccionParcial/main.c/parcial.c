    /*char producto[5][20];
    char aux[50];
    printf("Ingrese producto ");
    gets(aux);

    while(strlen(aux)>19){
        printf("El producto debe...");
        gets(aux);
    }
    strcpy(producto[4], aux);

    char aux[25];
    for(int i=0; i<14;i++){
        for(int j=i+1;j<15;j++){
            if(strcmp(nombre[i],nombre[j]) > 0){
                strcpy(aux, nombres[i]);
                strcpy(nombres[j], aux);
            }
        }
    }

    void mostrarVector(int x[]){
        int i;
        int j;
        for(i = 0; i<19; i++){
            for(j=i+1;j<20;j++){
                if(x[i]>x[j]){
                    aux = x[i];
                    x[i] = x[j];
                    x[j] = aux;
                }
            }
        }
    }*/
